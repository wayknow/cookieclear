// CookieClear Service Worker
// Reserved for future use (e.g., scheduled cleanup, alarm handlers).
// Currently no background tasks needed — everything is popup-driven.

console.log('CookieClear service worker started.');
